<li>
    {{burger_name}}

    <button class="eatburger" data-id="{{id}}" data-newBurger="{{devoured}}">
        {{#if devoured}}NOTHING BUT CRUMBS!{{else}}EAT THE BURGER!{{/if}}
    </button>

    <button class="removerburger" data-id="{{this.id}}">DELETE BURGERS!</button>
</li>